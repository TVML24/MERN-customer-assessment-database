import React from 'react';
import { Navigate, useParams } from 'react-router-dom';
import { useQuery } from '@apollo/client';

const AddRule = () => {

  return (
    <main>
      The Page to add a Rule
    </main>
  );
};

export default AddRule;
