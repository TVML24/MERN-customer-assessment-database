import React from 'react';
import { Navigate, useParams } from 'react-router-dom';
import { useQuery } from '@apollo/client';

const ViewRules = () => {

  return (
    <main>
      The Page to View All the Rules
    </main>
  );
};

export default ViewRules;
